# ASL ML App
An application to identify american sign language images using machine learning.

## Setup
To replicate the project environment, create an virtual environment and run the below steps,
* `Install setupwheels`

* ```Shell
    python3 -m pip install -e .
  ```

The above command installs the packages from replicates the environment using setup.py